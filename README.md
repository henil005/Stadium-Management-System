# Stadium-Management-System
**Stadium Management System** manages stadium operations like events, ticketing, and seating using **SQL** for data storage. The frontend is built in **Java** with **data structures** for efficient data handling and processing.
